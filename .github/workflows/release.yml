name: Release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: ubuntu-latest
            target: linux
            args: ""
          - platform: windows-latest
            target: windows
            args: ""
          - platform: macos-latest
            target: macos
            args: "--target universal-apple-darwin"

    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Linux system dependencies
        if: matrix.target == 'linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: latest
          run_install: false

      - name: Get pnpm store directory
        shell: bash
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Cache pnpm dependencies
        uses: actions/cache@v4
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target == 'macos' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: Cache Rust build artifacts
        uses: swatinem/rust-cache@v2
        with:
          workspaces: src-tauri -> target

      - name: Install frontend dependencies
        run: pnpm install

      - name: Build and release with Tauri
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: "Eqraa Desktop ${{ github.ref_name }}"
          args: ${{ matrix.args }}
          releaseBody: |
            ## What's New in v0.2.0

            ### New Features
            - Analytics charts added to Home and Teachers views
            - CSV and PDF export for Students, Attendance, and Quran pages
            - Auto-seed demo data on first app startup

            ### Bug Fixes
            - Fixed OOM crash in PDF export by replacing html2canvas with jspdf-autotable

            ---

            **Linux:** `.deb` / `.AppImage` / `.pkg.tar.zst` (Arch)
            **Windows:** `.msi` / `.exe`
            **macOS:** `.dmg`
          releaseDraft: false
          prerelease: false

  arch-package:
    name: Arch Linux Package
    runs-on: ubuntu-latest
    container: archlinux:latest
    needs: [build]

    steps:
      - name: Install build dependencies
        run: |
          pacman -Syu --noconfirm
          pacman -S --noconfirm base-devel git rust nodejs pnpm \
            webkit2gtk-4.1 gtk3 libayatana-appindicator librsvg patchelf

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build package
        run: |
          useradd -m builder
          chown -R builder:builder .
          cd pkg/arch
          sed -i 's|source=.*|source=()|' PKGBUILD
          sed -i 's|sha256sums=.*|sha256sums=()|' PKGBUILD
          sed -i "s|cd \"\$pkgname-\$pkgver\"|cd $GITHUB_WORKSPACE|g" PKGBUILD
          su builder -c "makepkg -sf --noconfirm"

      - name: Upload to release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          pacman -S --noconfirm github-cli
          gh release upload "${{ github.ref_name }}" \
            pkg/arch/eqraa-desktop-*.pkg.tar.zst \
            --clobber
